PORT=8080

# API storage location (used only when running api locally, not required for docker tmpfs)
SAVES_DIR=./saves

# “Security by obscurity”: shared passphrase header for /api/saves/*
SAVES_PASSPHRASE=change-me
